<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/g2">

    <ImageView
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:id="@+id/timetableimgview"
        android:scaleType="matrix"
        android:adjustViewBounds="true"
        android:minHeight="50dp"
        android:minWidth="50dp"
        />
    <ProgressBar
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:id="@+id/imgloadprogress"
        />
    <android.support.design.widget.FloatingActionButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginRight="50dp"
        android:layout_marginBottom="50dp"
        android:elevation="30dp"
        android:src="@drawable/timetableloadingicon"
        app:fabSize="normal"
        android:layout_above="@+id/timetableimgview"
        app:backgroundTint="#00af8f"
        android:id="@+id/reloadtimetable"
        />
</RelativeLayout>